{% extends 'base.html.twig' %}

{% block title %}Quack index{% endblock %}

{% block body %}
    <h1>Quack index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Auteur</th>
                <th>Titre</th>
                <th>Contenu</th>
                <th>Date de création</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for quack in quacks %}
            <tr>
                <td>{{ quack.duck.duckname}}</td>
                <td>{{ quack.title}}</td>
                <td>{{ quack.content }}</td>
                <td>{{ quack.createdAt ? quack.createdAt|date('Y-m-d H:i:s') : '' }}</td>
{#                {% if quack.duck.id == app.user.id %}#}
                <td>
                    <a href="{{ path('quack_show', {'id': quack.id}) }}">show</a>
                    <a href="{{ path('quack_edit', {'id': quack.id}) }}">edit</a>
                </td>
{#                {% endif %}#}
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
